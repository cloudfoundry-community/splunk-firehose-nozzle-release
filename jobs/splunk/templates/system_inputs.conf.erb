[default]
host = <%= spec.name %>_<%= spec.index %>_<%= spec.id %>

# todo: ssl -> configure both sides of cert (here + nozzle config)
# enableSSL = 1
# sslKeysfile =
# caCertFile =
# caPath =
# serverCert
# sslVersions =
# cipherSuite =
# requireClientCert =
